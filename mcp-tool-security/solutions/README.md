# Solution Reference

Three worked solutions:

- `hooks/pretooluse.py` — the full denial logic for the preToolUse hook
- `net_tools/server.py` — a `net-tools` MCP server with a domain allowlist enforced in the tool
- `TOOL_CLASSIFICATION.md` — one possible classification answer for the five tools

Compare against your own work; do not copy directly. The reasoning you record is the deliverable — the code is the artifact that proves the reasoning was applied.
